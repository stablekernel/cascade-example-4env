# AUTO-GENERATED by cascade - DO NOT EDIT MANUALLY
# Regenerate with: cascade generate-workflow --config .github/manifest.yaml
#
# Manifest-validation PR check (opt-in via validate_check.enabled).
# Runs `cascade parse-config` against the manifest on pull_request and
# fails when the configuration is invalid, so a malformed manifest cannot
# merge. Validates cascade's own configuration only; it does not run the
# repository's build or test suites.

name: Validate Manifest

on:
  pull_request:
    paths:
      - .github/manifest.yaml

permissions:
  contents: read

jobs:
  validate-manifest:
    name: Validate Manifest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      - name: Setup CLI
        uses: stablekernel/cascade/.github/actions/setup-cli@0b49ccc9e371d97e7bad2090e8ad96a911021168 # v0.14.0-dryrun.55
        with:
          version: v0.14.0-dryrun.55
          token: ${{ github.token }}
      - name: Validate Manifest
        run: |
          MANIFEST_FILE=".github/manifest.yaml"
          if [[ ! -f "$MANIFEST_FILE" ]]; then
            echo "::error::$MANIFEST_FILE not found"
            exit 1
          fi
          # parse-config reports validity in its JSON output (valid: false on
          # parse or schema errors) rather than via exit code, so gate on the
          # parsed result.
          RESULT=$(cascade parse-config --config "$MANIFEST_FILE")
          echo "$RESULT"
          VALID=$(echo "$RESULT" | jq -r '.valid // false')
          if [[ "$VALID" != "true" ]]; then
            echo "$RESULT" | jq -r '.errors[]? | "::error::" + .'
            echo "::error::Manifest validation failed"
            exit 1
          fi
          echo "::notice::Manifest is valid"
